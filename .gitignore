__pycache__
.claude
# Tauri / Rust build artefacts
tauri/src-tauri/target/
# Tauri build-script generated permission schemas — regenerated on every build
tauri/src-tauri/gen/schemas/
# PyInstaller build artefacts (the dist binary is committed by CI via artifact upload)
packaging/build/
packaging/dist/
.cursor
.pytest_cache
.ruff_cache
.serena
.venv
agent_workspace
.env
/logs/
server.log
/server.*.log
debug-*.log
.coverage
llama_cache
.smoke-results
tauri/companion-updater.key
tauri/companion-updater.key.pub

# OS / IDE
.DS_Store
**/.DS_Store

# Local PyInstaller / Tauri build artifacts (per-platform regenerated in CI)
/build/
/dist/
/packaging/build/
/packaging/dist/
